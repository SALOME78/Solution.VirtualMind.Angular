import { CurrencyPurchaseCreateModel } from './currency-purchase-create-model';

describe('CurrencyPurchaseCreateModel', () => {
  it('should create an instance', () => {
    expect(new CurrencyPurchaseCreateModel()).toBeTruthy();
  });
});
