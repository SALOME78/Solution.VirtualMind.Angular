import { ExchangeCurrencyModel } from './exchange-currency-model';

describe('ExchangeCurrencyModel', () => {
  it('should create an instance', () => {
    expect(new ExchangeCurrencyModel()).toBeTruthy();
  });
});
